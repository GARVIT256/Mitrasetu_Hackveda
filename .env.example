# Copy this file to .env and update with your actual credentials
# DO NOT commit .env to version control!

# ==============================================
# BACKEND ENVIRONMENT VARIABLES
# ==============================================

# Server Configuration
PORT=5000
NODE_ENV=production

# Database Configuration
# Replace with your MongoDB Atlas connection string
MONGO_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/mitrasetu

# Security Configuration
# Generate a secure random string (minimum 32 characters)
JWT_SECRET=your_super_secure_jwt_secret_key_minimum_32_characters_long
ENCRYPTION_KEY=your_32_character_encryption_key_here_exactly

# AWS Bedrock Configuration (for AI features)
# Get these from AWS IAM
AWS_ACCESS_KEY_ID=your_aws_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here
AWS_REGION=us-east-1
BEDROCK_MODEL_ID=amazon.nova-pro-v1:0

# CORS Configuration (comma-separated list of allowed origins)
CORS_ORIGINS=https://yourdomain.com,https://api.yourdomain.com

# ==============================================
# FRONTEND ENVIRONMENT VARIABLES
# ==============================================

# React Frontend
VITE_API_BASE_URL=https://api.yourdomain.com
VITE_LIVE_MODE=live

# ==============================================
# AWS CLI CONFIGURATION
# ==============================================

# Uncomment and fill these for AWS CLI if using ~/.aws/credentials
# [default]
# aws_access_key_id = your_access_key_here
# aws_secret_access_key = your_secret_key_here
# region = us-east-1

# ==============================================
# NOTES
# ==============================================

# 1. Never commit this file with real credentials to version control
# 2. Use strong, random passwords (minimum 32 characters)
# 3. Store credentials securely - use AWS Secrets Manager for production
# 4. Rotate credentials regularly
# 5. Use different credentials for different environments (dev/staging/prod)

# For AWS Bedrock Setup Guide, see: backend/AWS_SETUP.md
# For Complete AWS Deployment Guide, see: AWS_DEPLOYMENT_GUIDE.md